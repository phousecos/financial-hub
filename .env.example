# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service key for backend operations (bypasses RLS)
# Get this from Supabase Dashboard > Settings > API
SUPABASE_SERVICE_KEY=your-service-role-key

# Anthropic API Key for Claude Vision
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google Drive Configuration
# Get folder IDs from the Google Drive URL when viewing the folder
DRIVE_UNPROCESSED_FOLDER_ID=your-unprocessed-folder-id
DRIVE_PROCESSED_FOLDER_ID=your-processed-folder-id

# Google Service Account JSON (entire JSON content as a single line)
# Create a service account at: console.cloud.google.com > IAM & Admin > Service Accounts
# Download the JSON key and paste the entire content here (minified)
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Optional: Secret for securing the processor endpoint
# If set, requests must include: Authorization: Bearer <this-secret>
# Generate a random string: openssl rand -hex 32
PROCESSOR_SECRET=your-processor-secret
